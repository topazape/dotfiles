[custom.chezmoi]
command = 'echo chezmoi'
when = ''' test "$CHEZMOI" = 1 '''
symbol = ' '

[gcloud]
symbol = '󰬎 '
when = ''' test -n "$CLOUDSDK_ACTIVE_CONFIG_NAME" '''
format = '[$symbol$account(@$domain)  $project]($style) '

# [custom.gitmail]
# command = 'git config user.email'
# when = ''' test -d .git '''
# format = ' $output '
