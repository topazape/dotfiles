[custom.chezmoi]
command = 'echo chezmoi'
when = ''' test "$CHEZMOI" = 1 '''
symbol = ' '

# [custom.gitmail]
# command = 'git config user.email'
# when = ''' test -d .git '''
# format = ' $output '
