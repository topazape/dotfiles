[gcloud]
symbol = '󰯽 '
format = 'on [$symbol$account(@$domain) (\($project\))]($style) '

[golang]
symbol = '󰜉 '

[custom.chezmoi]
command = 'echo chezmoi'
when = ''' test "$CHEZMOI" = 1 '''
symbol = '🏡'
